name: Upstream Release Sync

on:
  schedule:
    - cron: '23 * * * *'
  workflow_dispatch:

concurrency:
  group: upstream-release-sync
  cancel-in-progress: false

permissions:
  contents: write
  actions: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      UPSTREAM_REPO: TNT-Likely/BeeCount
      DEFAULT_BRANCH: main
      RELEASE_WORKFLOW: release.yml
    steps:
      - name: Checkout fork
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Discover latest upstream release
        id: upstream
        shell: bash
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json, os, urllib.request
          repo = os.environ['UPSTREAM_REPO']
          headers = {
              'Accept': 'application/vnd.github+json',
              'User-Agent': 'github-actions'
          }

          def get_json(url):
              req = urllib.request.Request(url, headers=headers)
              with urllib.request.urlopen(req, timeout=30) as r:
                  return json.load(r)

          release = get_json(f'https://api.github.com/repos/{repo}/releases/latest')
          tag = release['tag_name']
          name = release.get('name') or tag
          ref = get_json(f'https://api.github.com/repos/{repo}/git/ref/tags/{tag}')
          target = ref['object']
          if target['type'] == 'tag':
              target = get_json(target['url'])['object']
          if target['type'] != 'commit':
              raise SystemExit(f'Expected tag {tag} to resolve to a commit, got {target["type"]}')

          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as fh:
              fh.write(f'tag_name={tag}\n')
              fh.write(f'release_name={name}\n')
              fh.write(f'tag_commit_sha={target["sha"]}\n')
          print(f'Latest upstream release: {tag} ({name}) -> {target["sha"]}')
          PY

      - name: Sync fork branch to upstream
        id: branch_sync
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git remote add upstream "https://github.com/${UPSTREAM_REPO}.git" 2>/dev/null || git remote set-url upstream "https://github.com/${UPSTREAM_REPO}.git"
          git fetch origin --prune --tags
          git fetch upstream --prune --tags
          git checkout "${DEFAULT_BRANCH}"
          TARGET_SHA='${{ steps.upstream.outputs.tag_commit_sha }}'
          CURRENT_SHA="$(git rev-parse HEAD)"
          echo "upstream_sha=${TARGET_SHA}" >> "$GITHUB_OUTPUT"
          echo "current_sha=${CURRENT_SHA}" >> "$GITHUB_OUTPUT"
          echo "Fork main:            ${CURRENT_SHA}"
          echo "Upstream release SHA: ${TARGET_SHA}"
          if git merge-base --is-ancestor "${TARGET_SHA}" HEAD; then
            echo 'branch_changed=false' >> "$GITHUB_OUTPUT"
            echo 'Fork already contains the latest upstream release commit.'
          else
            git merge --no-commit --no-ff "${TARGET_SHA}"
            git restore --source="${CURRENT_SHA}" --staged --worktree .github/workflows
            git commit --no-edit
            git push origin "${DEFAULT_BRANCH}"
            echo 'branch_changed=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Inspect existing fork tag
        id: current
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json, os, urllib.request, urllib.error

          tag = '${{ steps.upstream.outputs.tag_name }}'
          repo = os.environ['REPO']
          token = os.environ['GH_TOKEN']
          req = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/git/ref/tags/{tag}',
              headers={
                  'Authorization': f'Bearer {token}',
                  'Accept': 'application/vnd.github+json',
                  'X-GitHub-Api-Version': '2022-11-28',
                  'User-Agent': 'github-actions',
              },
          )

          exists = False
          sha = ''
          try:
              with urllib.request.urlopen(req, timeout=30) as r:
                  ref = json.load(r)
              target = ref['object']
              if target['type'] == 'tag':
                  tag_req = urllib.request.Request(target['url'], headers=req.headers)
                  with urllib.request.urlopen(tag_req, timeout=30) as r:
                      target = json.load(r)['object']
              if target['type'] != 'commit':
                  raise SystemExit(f'Expected fork tag {tag} to resolve to a commit, got {target["type"]}')
              exists = True
              sha = target['sha']
          except urllib.error.HTTPError as e:
              if e.code != 404:
                  body = e.read().decode() if e.fp else ''
                  raise SystemExit(f'Failed to inspect fork tag {tag}: {e.code} {body}')

          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as fh:
              fh.write(f'tag_exists={str(exists).lower()}\n')
              fh.write(f'tag_sha={sha}\n')
          if exists:
              print(f'Fork already has remote tag {tag} at {sha}')
          else:
              print(f'Fork does not have remote tag {tag}')
          PY

      - name: Decide whether release rebuild is needed
        id: decision
        shell: bash
        run: |
          set -euo pipefail
          NEEDS_BUILD=false
          if [[ '${{ steps.upstream.outputs.tag_commit_sha }}' != '${{ steps.current.outputs.tag_sha }}' ]]; then
            NEEDS_BUILD=true
          fi
          if [[ '${{ steps.current.outputs.tag_exists }}' != 'true' ]]; then
            NEEDS_BUILD=true
          fi
          echo "needs_build=${NEEDS_BUILD}" >> "$GITHUB_OUTPUT"
          echo "needs_build=${NEEDS_BUILD}"

      - name: Stop when fork already matches latest release
        if: steps.decision.outputs.needs_build != 'true'
        run: echo 'Fork already matches the latest upstream release; nothing to do.'

      - name: Delete old fork releases and tags
        if: steps.decision.outputs.needs_build == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json, os, urllib.request, urllib.error
          token = os.environ['GH_TOKEN']
          repo = os.environ['REPO']
          headers = {
              'Authorization': f'Bearer {token}',
              'Accept': 'application/vnd.github+json',
              'X-GitHub-Api-Version': '2022-11-28',
              'User-Agent': 'github-actions'
          }

          def request(method, url):
              req = urllib.request.Request(url, headers=headers, method=method)
              try:
                  with urllib.request.urlopen(req, timeout=60) as r:
                      body = r.read().decode() or ''
                      return r.status, body
              except urllib.error.HTTPError as e:
                  body = e.read().decode() if e.fp else ''
                  return e.code, body

          releases_url = f'https://api.github.com/repos/{repo}/releases?per_page=100'
          status, body = request('GET', releases_url)
          if status != 200:
              raise SystemExit(f'Failed to list releases: {status} {body}')
          releases = json.loads(body)
          for rel in releases:
              status, body = request('DELETE', f"https://api.github.com/repos/{repo}/releases/{rel['id']}")
              if status not in (204, 404):
                  raise SystemExit(f"Failed to delete release {rel['tag_name']}: {status} {body}")
              print(f"Deleted release: {rel['tag_name']}")

          tags_url = f'https://api.github.com/repos/{repo}/tags?per_page=100'
          status, body = request('GET', tags_url)
          if status != 200:
              raise SystemExit(f'Failed to list tags: {status} {body}')
          tags = json.loads(body)
          for tag in tags:
              name = tag['name']
              status, body = request('DELETE', f'https://api.github.com/repos/{repo}/git/refs/tags/{name}')
              if status not in (204, 404):
                  raise SystemExit(f'Failed to delete tag {name}: {status} {body}')
              print(f'Deleted tag: {name}')
          PY

      - name: Dispatch release build workflow
        if: steps.decision.outputs.needs_build == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          TAG_NAME: ${{ steps.upstream.outputs.tag_name }}
          RELEASE_NAME: ${{ steps.upstream.outputs.release_name }}
          WORKFLOW_FILE: ${{ env.RELEASE_WORKFLOW }}
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json, os, urllib.request
          token = os.environ['GH_TOKEN']
          repo = os.environ['REPO']
          workflow = os.environ['WORKFLOW_FILE']
          payload = {
              'ref': 'main',
              'inputs': {
                  'tag_name': os.environ['TAG_NAME'],
                  'release_name': os.environ['RELEASE_NAME'],
                  'prerelease': 'false',
                  'create_release': 'true'
              }
          }
          req = urllib.request.Request(
              f'https://api.github.com/repos/{repo}/actions/workflows/{workflow}/dispatches',
              data=json.dumps(payload).encode(),
              headers={
                  'Authorization': f'Bearer {token}',
                  'Accept': 'application/vnd.github+json',
                  'X-GitHub-Api-Version': '2022-11-28',
                  'User-Agent': 'github-actions',
                  'Content-Type': 'application/json'
              },
              method='POST'
          )
          with urllib.request.urlopen(req, timeout=60) as r:
              print(f'Dispatch status: {r.status}')
          PY
